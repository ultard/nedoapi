# 🎭 Генератор Мемов - Express.js API

Контрольная работа по Express.js: полноценное приложение для генерации мемов с текстом на изображениях.

## 📋 Описание проекта

Генератор мемов - это веб-приложение на Express.js, которое позволяет:
- Загружать изображения
- Добавлять текст сверху и снизу изображения
- Просматривать созданные мемы
- Редактировать текст существующих мемов
- Удалять мемы

## 🛠 Технологии

- **Node.js** - среда выполнения
- **Express.js** - веб-фреймворк
- **Multer** - обработка загрузки файлов
- **Canvas** - генерация изображений с текстом
- **UUID** - генерация уникальных идентификаторов

## 📁 Структура проекта

```
nedoapi/
├── server.js                 # Главный файл сервера
├── package.json              # Зависимости проекта
├── routes/
│   └── memes.js             # Маршруты API для мемов
├── controllers/
│   └── memesController.js   # Логика обработки запросов
├── middleware/
│   ├── logger.js            # Пользовательский middleware для логирования
│   ├── upload.js            # Настройка multer для загрузки файлов
│   └── validator.js         # Валидация данных
├── public/                  # Статические файлы
│   ├── index.html          # Главная страница
│   ├── styles.css          # Стили
│   └── script.js           # Клиентский JavaScript
├── uploads/                # Загруженные изображения (создаётся автоматически)
└── data/                   # Данные (создаётся автоматически)
    └── memes.json          # JSON файл с информацией о мемах
```

## 🚀 Установка и запуск

### 1. Установка системных зависимостей (для canvas)

**macOS:**
```bash
brew install pkg-config cairo pango libpng jpeg giflib librsvg
```

**Ubuntu/Debian:**
```bash
sudo apt-get install build-essential libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev
```

**Windows:**
Установите [GTK for Windows](https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer)

### 2. Установка зависимостей Node.js

```bash
npm install
```

### 3. Запуск сервера

```bash
npm start
```

Для разработки с автоматической перезагрузкой:

```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

## 📡 API Endpoints

### GET `/api/memes`
Получить список всех мемов с поддержкой пагинации.

**Query-параметры:**
- `limit` (опционально) - количество мемов (по умолчанию: все)
- `offset` (опционально) - смещение для пагинации (по умолчанию: 0)

**Пример:**
```bash
GET /api/memes?limit=10&offset=0
```

**Ответ:**
```json
{
  "total": 25,
  "limit": 10,
  "offset": 0,
  "memes": [...]
}
```

### GET `/api/memes/:id`
Получить конкретный мем по ID.

**Параметры пути (req.params):**
- `id` - уникальный идентификатор мема

**Пример:**
```bash
GET /api/memes/123e4567-e89b-12d3-a456-426614174000
```

### POST `/api/memes`
Создать новый мем.

**Тело запроса (multipart/form-data):**
- `image` (file) - изображение (обязательно)
- `topText` (string) - верхний текст (опционально)
- `bottomText` (string) - нижний текст (опционально)

**Пример:**
```bash
POST /api/memes
Content-Type: multipart/form-data

image: [файл]
topText: "Когда видишь баг"
bottomText: "Но он не воспроизводится"
```

### PUT `/api/memes/:id`
Обновить текст мема.

**Тело запроса (application/json):**
```json
{
  "topText": "Новый верхний текст",
  "bottomText": "Новый нижний текст"
}
```

### DELETE `/api/memes/:id`
Удалить мем.

## 🔧 Реализованные требования

### ✅ Базовый Express-сервер
- Сервер настроен в `server.js`
- Порт настраивается через переменную окружения `PORT` (по умолчанию 3000)

### ✅ Маршруты
- **GET** `/api/memes` - получение списка мемов
- **GET** `/api/memes/:id` - получение мема по ID
- **POST** `/api/memes` - создание мема
- **PUT** `/api/memes/:id` - обновление мема
- **DELETE** `/api/memes/:id` - удаление мема

### ✅ Работа с параметрами
- **req.params** - используется в маршрутах `/:id` для получения ID мема
- **req.query** - используется для пагинации (`?limit=10&offset=0`)

### ✅ Обработка тела запроса
- **express.json()** - для обработки JSON (PUT запросы)
- **express.urlencoded()** - для обработки URL-encoded данных
- **multer** - для обработки multipart/form-data (загрузка файлов)

### ✅ Пользовательский middleware
- **requestLogger** (`middleware/logger.js`) - логирует все входящие запросы с временем выполнения
- **validateMemeData** (`middleware/validator.js`) - валидирует данные мема

### ✅ Раздача статических файлов
- Используется `express.static()` для папки `public/`
- HTML, CSS и JavaScript доступны напрямую

### ✅ Модульная структура
- **routes/** - маршруты API
- **controllers/** - бизнес-логика
- **middleware/** - пользовательские middleware
- Разделение ответственности между слоями

## 🎨 Особенности реализации

1. **Генерация мемов**: Используется библиотека `canvas` для наложения текста на изображения
2. **Хранение данных**: JSON файл для простоты (можно заменить на БД)
3. **Валидация**: Проверка типов файлов, размера и длины текста
4. **Обработка ошибок**: Централизованная обработка ошибок
5. **Логирование**: Детальное логирование всех запросов

## 📸 Скриншоты

Для демонстрации работы приложения:

1. Откройте `http://localhost:3000` в браузере
2. Загрузите изображение и добавьте текст
3. Просмотрите созданные мемы
4. Протестируйте редактирование и удаление

## 📝 Примеры использования API

### Создание мема через curl:

```bash
curl -X POST http://localhost:3000/api/memes \
  -F "image=@/path/to/image.jpg" \
  -F "topText=Когда видишь баг" \
  -F "bottomText=Но он не воспроизводится"
```

### Получение всех мемов:

```bash
curl http://localhost:3000/api/memes
```

### Получение мема по ID:

```bash
curl http://localhost:3000/api/memes/{id}
```

### Обновление мема:

```bash
curl -X PUT http://localhost:3000/api/memes/{id} \
  -H "Content-Type: application/json" \
  -d '{"topText":"Новый текст","bottomText":"Ещё текст"}'
```

### Удаление мема:

```bash
curl -X DELETE http://localhost:3000/api/memes/{id}
```

## 🔒 Ограничения

- Максимальный размер файла: 5MB
- Поддерживаемые форматы: JPEG, JPG, PNG, GIF, WEBP
- Максимальная длина текста: 100 символов

## 📚 Дополнительная информация

Проект демонстрирует:
- Создание RESTful API
- Обработку файлов
- Работу с параметрами и query-строками
- Создание пользовательских middleware
- Модульную архитектуру
- Обработку ошибок
- Раздачу статических файлов

## 👨‍💻 Автор

Контрольная работа по Express.js

## 📄 Лицензия

ISC

